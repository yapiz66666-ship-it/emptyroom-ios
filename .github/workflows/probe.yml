# Temporary: checks which runners this account can start. Removed once diagnosed.
name: Probe
on: [push]
jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - run: echo linux ok
  mac:
    runs-on: macos-15
    steps:
      - run: sw_vers && xcodebuild -version
